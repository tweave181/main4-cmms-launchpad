## CI & Quality

- CI runs on PRs and main: build, lint, test (see `.github/workflows/ci.yml`)
- Dependabot weekly updates for npm, actions, and Docker
- Branch protection recommended for `main` (require PR + passing checks)
